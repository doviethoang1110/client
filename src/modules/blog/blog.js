export default class Blog {
  constructor(id,title,slug,content,image,status) {
    this.id = id;
    this.title = title;
    this.slug = slug;
    this.content = content;
    this.image = image;
    this.status = status;
  }
}
